import styled from "styled-components";
import css from "@styled-system/css";

const NavbarWrapperInner = styled("div")(
    css({
        height: "100%",
        width: "100%",
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
    })
);

export default NavbarWrapperInner;
