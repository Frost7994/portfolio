import styled from "styled-components";
import css from "@styled-system/css";

const Page = styled("div")(
    css({
        position: "relative",
        height: "100vh",
        width: "100%",
    })
);

export default Page;
